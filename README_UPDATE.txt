Publication taxonomy update

Upload/replace these four files in jlblancoupm.github.io:

  _data/publications.yml
  _data/taxonomy.yml   (new file)
  publications.html
  assets/css/main.css

This replaces the old generic `topics` field with three shared dimensions:
  methods
  systems
  applications

The Publications page adds filters for Method, System and Area and displays the taxonomy tags under each publication.
